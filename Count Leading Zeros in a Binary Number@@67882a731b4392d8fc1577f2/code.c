#include <stdio.h>
#include <limits.h>


int main() {
    int a,count;
    scanf("%d",&a);
    count=0;
    while(!(a&(~INT_MAX));
    {
        count++;
        num<<=1;

    }
    printf("%d",count);
    return 0;
}